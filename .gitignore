# Design specs stay local and are never committed
docs/specs/

# Environment files (only the documented example is committed)
.env
.env.*
!.env.example

.DS_Store

# Python
.venv
__pycache__/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# pnpm store created by `docker run ... pnpm install` from the repo root (frontend has its own for
# non-Docker installs)
.pnpm-store/
